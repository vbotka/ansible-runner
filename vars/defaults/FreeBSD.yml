---
# FreeBSD vars/defaults for ansible_runner

ar_packages: "{{ ar_packages_freebsd }}"
ar_links: "{{ ar_links_freebsd }}"
ar_pip_executable: "{{ pip_executable_freebsd }}"
ar_pip_requirements: "{{ pip_requirements_freebsd }}"

# EOF
...
