---
# Debian vars/defaults for ansible_runner

ar_packages: "{{ ar_packages_debian }}"
ar_links: "{{ ar_links_debian }}"
ar_pip_executable: "{{ pip_executable_debian }}"
ar_pip_requirements: "{{ pip_requirements_debian }}"

# EOF
...
